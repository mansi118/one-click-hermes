{
  "_comment": "NeuralEDGE MCP server registrations. Installer seeds this to ~/.hermes/mcp.json on first run (only if absent). Schema follows the Model Context Protocol standard; verify against Hermes' loader at hermes doctor time.",

  "mcpServers": {
    "cortex-mcp": {
      "transport": "sse",
      "url": "http://127.0.0.1:8765/sse",
      "description": "CORTEX-PALACE — NEOS long-term memory bridge (Tier-2). Tools: cortex_recall, cortex_remember, cortex_user_model, cortex_search_entities, cortex_status.",
      "enabled": true
    }
  }
}
